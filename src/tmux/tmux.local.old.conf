# Set mouse events
set -g focus-events on
set -g mouse on

# Set binding keys
bind P paste-buffer
bind-key -t vi-copy v begin-selection
bind-key -t vi-copy y copy-pipe 'cat | xclip -selection clipboard'
bind-key -t vi-copy r rectangle-toggle
